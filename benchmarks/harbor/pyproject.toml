[project]
name = "sabre-harbor-agent"
version = "0.1.0"
description = "SABRE agent for Harbor benchmarks"
requires-python = ">=3.12"
dependencies = ["harbor"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["container"]
